#ifndef STRING_H
#define STRING_H 
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

extern int m_strlen(const char *s);
extern char *reverse(char *s);
extern char *copy(char *dst, const char *src);
#endif
